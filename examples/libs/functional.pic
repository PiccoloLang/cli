
fn reduce(reducer, base) {
    fn arr -> {
        var result = base
        for x in arr
            result = reducer(result, x)
        result
    }
}

fn filter(predicate) {
    fn arr -> {
        var result = []
        for x in arr
            if predicate(x)
                result = result + [x]
        result
    }
}